version: '3.8'

services:
  web:
    image: flask:latest
    ports:
      - '5000:5000'
    volumes:
      - .:/code
    environment:
      - FLASK_ENV=development
      - FLASK_APP=app.py
    command: flask run --host=0.0.0.0 --port=5000
